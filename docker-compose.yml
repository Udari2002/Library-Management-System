version: '3.8'
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - lms-net
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    networks:
      - lms-net
networks:
  lms-net:
    driver: bridge
